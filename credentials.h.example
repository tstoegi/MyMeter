// Credentials template for MyMeter
// Copy this file to credentials.h and fill in your values
//
// NOTE: WiFi, MQTT, and OTA credentials are now configured via the web portal.
// On first boot (or 6x reset), connect to the "MyMeter-Setup" WiFi AP
// (or whatever name you set in config.h) and configure your settings.

// OTA (Over-The-Air) update password
// DEPRECATED: Configure via web portal instead (Settings page at 192.168.4.1)
// This define is no longer used in the code.
// #define CR_OTA_MYMETER_CLIENT_PASSWORD ""

// MQTT TLS fingerprint (only used if MQTT_TLS is enabled in config.h)
// Use "" to disable certificate checking
#define CR_MQTT_BROKER_CERT_FINGERPRINT ""
