// Credentials template for MyMeter
// Copy this file to credentials.h and fill in your values
//
// NOTE: WiFi and MQTT credentials can be configured in two ways:
// 1. Runtime via web portal (recommended for easy changes)
// 2. Compile-time via this file (for pre-configured devices)

// OTA (Over-The-Air) update password
#define CR_OTA_MYMETER_CLIENT_PASSWORD "esp8266"

// MQTT TLS fingerprint (only used if MQTT_TLS is enabled in config.h)
// Use "" to disable certificate checking
#define CR_MQTT_BROKER_CERT_FINGERPRINT ""

// ============================================================================
// OPTIONAL: Pre-configure device with default settings
// ============================================================================
// If these defines are present, the device will auto-create config.json
// on first boot with these values. This skips the config portal setup.
//
// Use case: Flash multiple devices with pre-configured settings
//
// To use:
// 1. Uncomment and fill in the values below
// 2. Compile and flash
// 3. Device will start immediately with these settings
//
// To force config portal instead: Comment out or delete these defines
// ============================================================================

// #define DEFAULT_WIFI_SSID "YourWiFiSSID"
// #define DEFAULT_WIFI_PASSWORD "YourWiFiPassword"
// #define DEFAULT_MQTT_BROKER "192.168.1.100"
// #define DEFAULT_MQTT_PORT 1883
// #define DEFAULT_MQTT_USER "mqtt_user"
// #define DEFAULT_MQTT_PASSWORD "mqtt_password"
// #define DEFAULT_MQTT_TOPIC "home/mymeter"
